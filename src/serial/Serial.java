package serial;

import java.io.*;
import java.util.ArrayList;


public class Serial {
	
	 public static void main(String args[]) { 
		 /*   
		   ArrayList lesPersonnes = new ArrayList<Personne>();
		   lesPersonnes.add( new Personne("Martin","Nicolas",1987));
		   lesPersonnes.add( new Personne("Thomas","Nicolas",1996));
		   lesPersonnes.add( new Personne("camille","Nicolas",1998));
		    try { 
		      FileOutputStream fichier = new FileOutputStream("personnes.obj"); 
		      ObjectOutputStream s = new ObjectOutputStream(fichier); 
		      s.writeObject(lesPersonnes); 
		      s.flush(); 
		      s.close(); 
		    } 
		    catch (IOException e) { 
		      System.out.println("Probleme IO"+ e); 
		    }*/
	 }
}
